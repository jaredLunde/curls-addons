import {string, bool, object} from 'prop-types'


export default {
  size: string,
  xxl: bool,
  xl: bool,
  lg: bool,
  md: bool,
  sm: bool,
  xs: bool,
  xxs: bool,
  name: string,
  containStrict: bool,
  icons: object
}
